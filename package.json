{
  "name": "umt-prompt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -r assets index.html dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
